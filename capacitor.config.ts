import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Botón Naranja',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
